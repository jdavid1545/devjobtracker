export type DisplayEntry = {
    type: String,
    company: String,
    timestamp: Date
}

export type RequestEntry = {
    email: String,
    type: String,
    company: String,
    timestamp: Date
}