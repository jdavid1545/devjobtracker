export type DisplayEntry = {
  type: String;
  company: String;
  timestamp: Date;
};

export type RequestEntry = {
  email: String;
  entryType: String;
  company: String;
  timestamp: Date;
};

export type emailProp = {
  email: string;
};
